import React from 'react';
import './Logo.scss';

const Logo = () => (
  <div className="logo">
    <span className="logo__span">
      DDyrebeskyttelsens
      <br />
      Bergen
    </span>
  </div>
);

export default Logo;
